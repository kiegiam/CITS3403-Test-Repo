Database migrations for FitTrack.

The version files in `versions/` document the schema changes needed to build
the application database from an empty SQLite database. They are written as
plain Alembic migration modules so the schema history is reviewable in source
control.
